import array from 'vtypes-array';
import arrayOf from 'vtypes-arrayof';
import bool from 'vtypes-bool';
import different from 'vtypes-different';
import func from 'vtypes-func';
import immutable from 'vtypes-immutable';
import invariant from 'vtypes-invariant';
import json from 'vtypes-json';
import objectOf from 'vtypes-objectof';
import only from 'vtypes-only';
import requiredIf from 'vtypes-requiredif';
import requiredUnless from 'vtypes-requiredunless';
import requiredWith from 'vtypes-requiredwith';
import requiredWithAll from 'vtypes-requiredwithall';
import requiredWithout from 'vtypes-requiredwithout';
import requiredWithoutAll from 'vtypes-requiredwithoutall';
import shape from 'vtypes-shape';
import size from 'vtypes-size';
import string from 'vtypes-string';

export default {
  array,
  arrayOf,
  bool,
  different,
  func,
  immutable,
  invariant,
  json,
  objectOf,
  only,
  requiredIf,
  requiredUnless,
  requiredWith,
  requiredWithAll,
  requiredWithout,
  requiredWithoutAll,
  shape,
  size,
  string
};
